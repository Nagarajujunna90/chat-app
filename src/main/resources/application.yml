server:
  port: 9090

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/chat-app
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: user

